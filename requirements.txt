# ── Core Framework ──
django>=5.2,<6.0
daphne>=4.1
channels>=4.1
channels-redis>=4.2

# ── Task Queue & Broker ──
celery>=5.4
redis>=5.0

# ── Database Drivers ──
psycopg2-binary>=2.9
dj-database-url>=2.1

# ── Environment & Config ──
python-dotenv>=1.0
pyyaml>=6.0

# ── Data & ML ──
# NOTE: torch is installed separately in build.sh (CPU-only to save space)
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
gymnasium>=0.29
tqdm>=4.66

# ── Trading & Market Data ──
alpaca-trade-api>=3.2
yfinance>=0.2
websockets

# ── NLP / Sentiment ──
requests>=2.31
beautifulsoup4>=4.12
transformers>=4.40

# ── Deployment ──
gunicorn>=22.0
whitenoise>=6.6